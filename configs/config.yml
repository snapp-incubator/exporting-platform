exporter:
  address: "<address for our exporter to serve on (host + port)>"
harbor:
  address: "<address of harbor (host + port)>"
  token_path: "<path to token file>"
keystone:
  clouds:
    - openstack_name: "<name cluster>"
      metric_name: "<name prefix>"